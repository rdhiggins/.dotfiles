#!/bin/bash

[[ -s ~/.bashrc ]] && source ~/.bashrc

# mise — manages Ruby, Python, Node, and other language versions
eval "$(mise activate bash)"

# Poetry (modern path)
export PATH="$HOME/.local/bin:$PATH"
