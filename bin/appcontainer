#!/usr/bin/env ruby

settings = `xcrun xcodebuild -showBuildSettings`

appid = settings.match('^    PRODUCT_BUNDLE_IDENTIFIER = (.*)$')[1]

container = %x[xcrun simctl get_app_container booted #{appid}]

if container == "" then
  puts Dir.pwd
else
  puts container
end
