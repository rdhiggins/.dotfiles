#!/usr/bin/env ruby

if File.exists?('/Applications/Xcode-beta.app') then
  unless (filename = Dir.glob("*.xc*").first).nil?
    puts "Opening #{filename}"
    %x(open -a /Applications/Xcode-beta.app #{filename})
  end
else
  puts "Xcode-beta is not installed"
end


